version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - echo "Building the project.."
      - npm run build
artifacts:
  files:
    - dist/**/*
    - appspec.yml
    - scripts/*
